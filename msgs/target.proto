syntax = "proto2";
package grasp.msgs;

/// \ingroup grasp_msgs
/// \interface Target
/// \brief TODO

message Target
{
    /// Type of PID control target
    enum Type
    {
        /// Position target
        POSITION = 1;
        /// Velocity target
        VELOCITY = 2;
    }

    /// Type of PID control target
    required Type       type    = 1;
    /// Joint name
    required string     joint   = 2;
    /// Target
    required double     value   = 3;
}
